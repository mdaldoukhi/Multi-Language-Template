/*
    Will handle all pages which means when call multi screens no need to have in two lines, will be in one lines
*/
import HomeScreen from "./HomeScreen";
import LanguageScreen from "./LanguageScreen";

export { HomeScreen, LanguageScreen };
